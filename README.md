# War Game (FastAPI + React)

A simple implementation of the classic **War card game**, with:

- **Python FastAPI backend** (game logic, deck handling, API endpoints)
- **React + TypeScript frontend** (UI to play rounds, resolve WAR, view decks)

---

## 🗂 Project Structure
